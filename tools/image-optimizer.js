#!/usr/bin/env node
/**
 * Optional batch image optimization for assets/ (e.g. sharp-cli, squoosh-cli, cwebp).
 * Example CLI (adapt paths): npx sharp-cli -i assets/thumbnails -o dist --webp quality=82
 */
console.log("Add your preferred optimizer pipeline; see README.md Asset layout.");
process.exit(0);
